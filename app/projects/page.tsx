import { genPageMetadata } from 'app/seo'

export const metadata = genPageMetadata({ title: 'Projects' })

const Grafana = 'This website will be display Grafana and Access Records.'
export default function Projects() {
  return Grafana
}
